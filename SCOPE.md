# SCOPE.md — FlowU

> ขอบเขตโครงงาน Module 2 (สัปดาห์ 6–9) · ADT-RAISE Non-Degree Batch 2
> ⚠️ ช่องที่มี `[ต้องเติม]` คือส่วนที่ต้องกรอกให้ตรงกับกระบวนการจริงของหน่วยงาน

---

## ประโยคขอบเขต (ข้อ 2️⃣ ของคู่มือ)

> ระบบของฉันเก็บ **ใบคำร้องนักศึกษาฝึกงาน** ที่ **นักศึกษา** สร้างขึ้น
> แต่ละใบเลือก **ประเภทคำร้อง** ได้ และมีสถานะ **รอพิจารณา → ตีกลับให้แก้ไข / อนุมัติ / ไม่อนุมัติ**
> โดย **ผู้พิจารณาตามลำดับชั้น** เป็นคนกดเปลี่ยน

---

## ตารางขอบเขต 9 บรรทัด (ข้อ 6️⃣ ของคู่มือ)

| | 🔧 LeaveEasy | 👤 FlowU |
|---|---|---|
| 📁 โฟลเดอร์หลัก | `leaveRequests` | `internshipRequests` |
| 📁 โฟลเดอร์ประเภท | `leaveTypes` | `requestTypes` |
| 📁 โฟลเดอร์ย่อย | `approvals` | `approvals` (ความเห็นผู้พิจารณาทีละระดับ) |
| ✏️ ช่องบอกว่าเป็นของใคร | `requesterId` · `requesterName` | `studentId` · `studentName` |
| 🔀 สถานะทั้งหมด | รอพิจารณา · อนุมัติ · ไม่อนุมัติ | รอพิจารณา · ตีกลับให้แก้ไข · อนุมัติ · ไม่อนุมัติ **(4 ค่า — ดูหมายเหตุ)** |
| 👤 คนที่สร้างรายการ | พนักงาน | นักศึกษา |
| 👤 คนที่เปลี่ยนสถานะ | หัวหน้า | ผู้พิจารณา (`approver`) |
| 📝 ช่องข้อความยาวที่ AI จะอ่าน | `reason` | `detail` (รายละเอียด/เหตุผลของคำร้อง) |
| 🤖 งานที่ AI ช่วย (สัปดาห์ที่ 8) | จัดประเภทการลาให้อัตโนมัติ | ระดับ 1: จัดประเภทคำร้องอัตโนมัติ · ระดับ 2: สรุปความเห็นผู้อนุมัติทุกระดับให้นักศึกษาอ่านรอบเดียว |

---

## เช็ก 6 อย่างที่ Module 2 ต้องใช้ (ข้อ 1️⃣ ของคู่มือ)

| # | ต้องมี | FlowU มีไหม |
|:-:|---|---|
| 1 | รายการหลักที่เพิ่มเข้ามาเรื่อย ๆ | ✅ `internshipRequests` |
| 2 | ช่องบอกว่ารายการนี้เป็นของใคร | ✅ `studentId` |
| 3 | สถานะที่เปลี่ยนได้ | ✅ 3 สถานะ |
| 4 | ประเภทที่เลือกจากรายการ | ✅ `requestTypes` |
| 5 | โฟลเดอร์ย่อยผูกกับรายการนั้น | ✅ `approvals` |
| 6 | ช่องข้อความยาวที่คนพิมพ์เอง | ✅ `detail` |

**ครบ 6 ข้อ → ลงมือได้**

---

## บทบาทผู้ใช้ (3 บทบาท — ตามเพดานของคู่มือ)

| บทบาท | ค่าใน `role` | ทำอะไรได้ |
|---|---|---|
| 🎓 นักศึกษา | `student` | ยื่นคำร้องใหม่ · ดูคำร้องของตัวเอง · ติดตามสถานะ · ลบคำร้องที่ยังเป็น `รอพิจารณา` |
| ✅ ผู้อนุมัติ | `approver` | ดูคำร้องที่รอระดับของตัวเอง · กดอนุมัติ / ส่งกลับแก้ · เขียนความเห็น |
| 🧾 เจ้าหน้าที่ | `staff` | ดูคำร้องทุกใบ · จัดการ `requestTypes` · กำหนดสายอนุมัติ |

> 📌 ผู้บริหารเป็นผู้ใช้ในเฟส Dashboard (Module 3) — ยังไม่นับเป็นบทบาทใน Module 2

---

## หน้าจอ (4 หน้า — ตามเพดานของคู่มือ)

1. `requests.html` — รายการคำร้อง (แสดงสถานะ + ระดับที่ค้างอยู่)
2. `new-request.html` — ฟอร์มยื่นคำร้องใหม่
3. `request-detail.html` — รายละเอียด + timeline การอนุมัติ + ปุ่มอนุมัติ
4. `request-types.html` — จัดการประเภทคำร้อง (เจ้าหน้าที่)

---

## โครงสร้างข้อมูล

```
internshipRequests/{requestId}
  title            หัวข้อคำร้อง
  detail           รายละเอียด/เหตุผล  ← AI อ่านช่องนี้
  typeId           อ้างไป requestTypes
  typeName         จดชื่อซ้ำไว้ (denormalize)
  studentId        uid ของนักศึกษาที่ล็อกอิน
  studentName      จดชื่อซ้ำไว้ (denormalize)
  status           รอพิจารณา | ตีกลับให้แก้ไข | อนุมัติ | ไม่อนุมัติ
  approvalChain    array  ← สายอนุมัติที่คัดลอกไว้ตอนยื่น
  currentLevel     ระดับที่กำลังรอพิจารณา
  currentLevelSince  มาถึงระดับนี้เมื่อไหร่ (เตือนเมื่อเกิน 24 ชม. · เป้าหมายรวม 3 วัน/รอบ)
  totalLevels      จำนวนระดับทั้งหมดของใบนี้
  round            รอบการยื่น (เพิ่มขึ้นเมื่อถูกตีกลับแล้วยื่นใหม่)
  returnReason     เหตุผลที่ถูกตีกลับ
  createdAt        วันเวลาที่ยื่น
  aiSuggestion     ผลจาก AI (สัปดาห์ 8)
  aiReason         เหตุผลที่ AI ให้ (สัปดาห์ 8)

  approvals/{approvalId}        ← โฟลเดอร์ย่อย
    round          รอบการยื่น
    level          ระดับ
    approverName   ชื่อผู้พิจารณา
    decision       เห็นด้วย | ไม่เห็นด้วย | อนุมัติ | ไม่อนุมัติ
    comment        ความเห็น
    createdAt      วันเวลา

requestTypes/{typeId}
  name             ชื่อประเภทคำร้อง
  approvalChain    array  ← สายอนุมัติของประเภทนี้ (แก้ได้)

users/{uid}
  name             ชื่อ
  role             student | approver | staff
  advisorId        อาจารย์ที่ปรึกษา (มาจาก REG)
  level            [ต้องเติม] ระดับที่ผู้อนุมัติคนนี้รับผิดชอบ (1–6)
```

---

## สายอนุมัติ 6 ระดับ

อ้างอิงใบคำร้องกระดาษหน้า 2

| ระดับ | ผู้พิจารณา | ประเภทการตัดสิน |
|:-:|---|---|
| 1 | อาจารย์ที่ปรึกษา | เห็นด้วย / ไม่เห็นด้วย |
| 2 | อาจารย์ที่ปรึกษาการฝึกปฏิบัติงาน | เห็นด้วย / ไม่เห็นด้วย |
| 3 | คณบดี | เห็นด้วย / ไม่เห็นด้วย |
| 4 | เจ้าหน้าที่ส่วนจัดหางานและฝึกงาน | เห็นด้วย / ไม่เห็นด้วย |
| 5 | หัวหน้าส่วนจัดหางานและฝึกงาน | เห็นด้วย / ไม่เห็นด้วย |
| 6 | อธิการบดี | **อนุมัติ / ไม่อนุมัติ** + กำหนดชั่วโมงบำเพ็ญประโยชน์ |

⭐ **ระดับ 1–5 ให้ความเห็น ระดับ 6 ตัดสิน**

- ระดับ 1–5 กด **เห็นด้วย** → เดินต่อระดับถัดไป `status` ยังเป็น `รอพิจารณา`
- ระดับ 1–5 กด **ไม่เห็นด้วย** → `status = ตีกลับให้แก้ไข` พร้อมเหตุผล (บังคับกรอก) นักศึกษาแก้แล้วยื่นใหม่ เริ่มรอบใหม่ที่ระดับ 1 · ยื่นแก้ซ้ำได้ไม่จำกัดรอบ
- ระดับ 6 เท่านั้นที่เปลี่ยน `status` เป็น `อนุมัติ` / `ไม่อนุมัติ`

### ⚠️ หมายเหตุเรื่องจำนวนสถานะ

คู่มือขอบเขตระบุว่าจำนวนสถานะที่พอดีคือ **3** แต่กระบวนการจริงของหน่วยงานมีการตีกลับให้นักศึกษาแก้ไข
ซึ่งเป็นหัวใจของโจทย์ (ลดเวลาที่เสียไปกับการเดินเก็บลายเซ็นใหม่) จึงใช้ **4 สถานะ**

เกินคู่มือ 1 ค่า แต่ไม่กระทบเกณฑ์ผ่าน Module 2 ทั้ง 6 ข้อ ซึ่งไม่ได้วัดจำนวนสถานะ
และ `ตีกลับให้แก้ไข` เป็นสถานะที่ทำให้เกิดปุ่ม **แก้ไข** ซึ่งเป็นตัว U ของ CRUD ที่การบ้านที่ 2 บังคับอยู่แล้ว

📌 **ควรแจ้งอาจารย์ตอน Consult** ว่าเลือกใช้ 4 สถานะเพราะเหตุนี้

### 🔧 สายอนุมัติต้องแก้ได้

หน่วยงานแจ้งว่าในอนาคตอาจไม่ต้องผ่านครบทุกระดับ จึงห้ามเขียนเลข 6 ตายในโค้ด
เก็บสายอนุมัติเป็น array ในแต่ละ `requestTypes` แล้วคัดลอกมาไว้ในคำร้องตอนยื่น
(ไม่สร้างโฟลเดอร์ใหม่ จึงยังอยู่ในเพดาน 1 หลัก + 2 ประกอบ + 1 ย่อย)

---

## ช่องในแบบฟอร์มคำร้อง

อ้างอิงใบคำร้องกระดาษหน้า 1

| ช่อง | ชื่อในระบบ | บังคับกรอก |
|---|---|:-:|
| ชื่อ-สกุล | `studentName` | ระบบเติมจาก REG |
| รหัสประจำตัว | `studentCode` | ระบบเติมจาก REG |
| เบอร์โทรศัพท์ติดต่อ | `phone` | ระบบเติม (แก้ได้) |
| อีเมล | `email` | ระบบเติมจาก REG |
| สำนักวิชา | `schoolName` | ระบบเติมจาก REG |
| สาขาวิชา | `programName` | ระบบเติมจาก REG |
| ภาคการศึกษา (ต้น / ปลาย) | `semester` | ✅ |
| ปีการศึกษา | `academicYear` | ✅ |
| ความประสงค์ (5 ประเภท) | `typeId` · `typeName` | ✅ |
| ข้อมูลเพิ่มตามประเภท | `typeDetail` | |
| เหตุผลอย่างละเอียด | `reason` ← AI อ่านช่องนี้ | ✅ |
| ชั่วโมงบำเพ็ญประโยชน์ (เสนอ) | `penaltyHoursProposed` | ระดับ 3 กรอก |
| ชั่วโมงบำเพ็ญประโยชน์ (อนุมัติ) | `penaltyHoursApproved` | ระดับ 6 กรอก |

> ข้อมูลนักศึกษายึดตามระบบ REG ของมหาวิทยาลัย นักศึกษาไม่ต้องพิมพ์เอง
> เฟสนี้เจ้าหน้าที่นำเข้าข้อมูลลง `users` ด้วยมือ ส่วนการเชื่อม REG อยู่ใน Backlog

### ประเภทคำร้อง 5 แบบ (`requestTypes`)

1. ขอซ่อมกิจกรรมเตรียมความพร้อมก่อนการฝึกปฏิบัติงาน (Pre-internship)
2. ขอยื่นคำร้องฝึกปฏิบัติงานล่าช้า
3. ขอย้ายการฝึกปฏิบัติงานจากสถานประกอบการเดิม
4. ขอยกเลิกการฝึกปฏิบัติงาน
5. อื่น ๆ (โปรดระบุ)

---

## นอกขอบเขต Module 2 (ย้ายไป `BACKLOG.md`)

- SSO ด้วยบัญชีมหาวิทยาลัย → ใช้ Firebase Auth email/password ไปก่อน
- Dashboard ผู้บริหาร → Module 3
- แจ้งเตือนอีเมล / LINE
- แนบไฟล์เอกสารหลักฐาน (ฟอร์มเดิมกำหนดว่าต้องแนบ)
- ลายมือชื่อดิจิทัลที่มีผลทางกฎหมาย
- เชื่อมต่อ REG API (ต้องขออนุญาตส่วนทะเบียนฯ + ศูนย์เทคโนโลยีสารสนเทศ — เริ่มยื่นขอได้เลย)
- ส่งอีเมลแจ้งชั่วโมงบำเพ็ญประโยชน์ไปส่วนพัฒนานักศึกษาอัตโนมัติ
- แจ้งเตือนเมื่อคำร้องถึงคิว ถูกตีกลับ หรือค้างเกินกำหนด
- ขยายใช้กับหน่วยงานอื่น
- ย้ายคีย์ AI ไปฝั่งที่ผู้ใช้แตะไม่ได้ (บังคับต้องมีใน Backlog ตามการบ้าน 4)

---

## ข้อกำหนดทางเทคนิค (บังคับ ห้ามเปลี่ยน)

| หัวข้อ | ข้อกำหนด |
|---|---|
| หน้าเว็บ | HTML · CSS · JavaScript ธรรมดา — **ห้ามใช้ framework** |
| โครงไฟล์ | แยกไฟล์ `.html` หน้าละไฟล์ · `css/` · `js/` |
| เซิร์ฟเวอร์ | ไม่มี — หน้าเว็บคุยกับฐานข้อมูลตรง ๆ |
| ฐานข้อมูล | Firestore |
| ล็อกอิน | Firebase Authentication (Email/Password) |
| ความปลอดภัย | Firestore Security Rules |
| นำขึ้นออนไลน์ | Firebase Hosting |
| ภาษาบนหน้าจอ | ไทยทั้งหมด · ชื่อไฟล์และชื่อช่องเป็นอังกฤษ |
