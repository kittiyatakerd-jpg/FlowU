# ACL — สิทธิ์การใช้งานตามบทบาท (FlowU)

อ้างอิงจาก [SCOPE.md](SCOPE.md) หัวข้อ "บทบาทผู้ใช้" — ค่าที่เก็บในช่อง `role` ของโฟลเดอร์ `users`
ต้องเป็นรหัสภาษาอังกฤษเสมอ (`student` / `approver` / `staff`)

| บทบาท | `role` | ทำอะไรได้ | ทำอะไรไม่ได้ |
|---|---|---|---|
| 🎓 นักศึกษา | `student` | ยื่นคำร้องใหม่ · ดูคำร้อง**ของตัวเอง** · แก้ไขและยื่นใหม่เมื่อสถานะเป็น `ตีกลับให้แก้ไข` · ลบคำร้องของตัวเองที่สถานะยังเป็น `รอพิจารณา` | เปลี่ยนสถานะคำร้องใดๆ (ให้ความเห็น/ตัดสิน) · **เปิดดูคำร้องของนักศึกษาคนอื่น** · แก้ไขประเภทคำร้อง · แก้ไขคำร้องที่ไม่ใช่สถานะ `ตีกลับให้แก้ไข` |
| ✅ ผู้พิจารณา | `approver` | ดูคำร้องที่ถึงคิวระดับของตัวเอง (`approvalLevel == currentLevel`) · ให้ความเห็น (เห็นด้วย/ไม่เห็นด้วย) ถ้าระดับเป็น `opinion` · ตัดสินอนุมัติ/ไม่อนุมัติถ้าระดับเป็น `decision` (ค่าเริ่มต้นคืออธิการบดี ระดับสุดท้าย) | **พิจารณาคำร้องที่ตัวเองเป็นผู้ยื่น** (`studentId == uid`) · แก้ไข/ลบความเห็นที่บันทึกไปแล้ว · เปลี่ยนสถานะคำร้องที่ไม่ถึงคิวระดับของตัวเอง · ลบคำร้องของนักศึกษา |
| 🧾 เจ้าหน้าที่ | `staff` | ทำได้ทุกอย่างที่ผู้พิจารณาทำได้ (ยกเว้นพิจารณาคำร้องตัวเอง) · เพิ่ม/แก้/ลบประเภทคำร้อง (`requestTypes`) และสายอนุมัติ (`approvalChain`) — *หน้าจอจัดการยังไม่ทำในสัปดาห์นี้ ทำผ่าน Firebase Console ไปก่อน* · ดูคำร้องทุกใบ | ลบความเห็นที่บันทึกไปแล้ว |

สมัครสมาชิกใหม่ทุกคนได้ `role: student` เป็นค่าเริ่มต้นเสมอ (ดู [js/login.js](js/login.js)) —
การเลื่อนเป็น `approver`/`staff` (และกำหนด `approvalLevel` ให้ผู้พิจารณา) ทำโดยแก้ไฟล์ผู้ใช้นั้นตรงในโฟลเดอร์ `users`
ผ่าน Firebase Console เท่านั้น ยังไม่มีหน้าจอจัดการสิทธิ์ในระบบ

## สถานะการบังคับใช้จริงในโค้ด

| สัปดาห์ | มีล็อกอินไหม | แยกสิทธิ์ตาม role ไหม |
|---|---|---|
| 6 | ไม่มี — ทุกหน้าเปิดใช้ได้หมด | ไม่มี |
| **7 (ตอนนี้)** | **มี — ต้องล็อกอินก่อนถึงเข้าหน้าไหนก็ได้** (`js/auth-guard.js`) และ Firestore Rules บังคับ `request.auth != null` กับทุกโฟลเดอร์ | **บังคับบางส่วนแล้วที่หน้าจอ** (`js/request-detail.js` ซ่อนปุ่มพิจารณาถ้า role/approvalLevel ไม่ตรง และกันผู้พิจารณาพิจารณาคำร้องตัวเอง) — **แต่ Security Rules ยังเป็นกฎเดียวกันหมด** (`auth != null` เท่านั้น) ยังไม่เช็ค role ฝั่งเซิร์ฟเวอร์ |
| 8 | เหมือนเดิม | จะเพิ่ม Security Rules ที่เช็ค `role`/`approvalLevel`/`studentId == request.auth.uid` จริงในฝั่งกฎ ไม่ใช่แค่ซ่อนปุ่ม |

⚠️ **ข้อจำกัดที่ต้องรู้ตอนนี้:** เพราะ Security Rules ยังไม่แยกตาม role จริง นักศึกษาคนหนึ่งที่ล็อกอินอยู่ยังสามารถ
เปิด URL ตรงไปที่คำร้องของนักศึกษาคนอื่นแล้วอ่านข้อมูลได้ (แค่ไม่เห็นปุ่มพิจารณา เพราะซ่อนที่หน้าจอเท่านั้น) —
เป็นข้อจำกัดที่ยอมรับได้สำหรับสัปดาห์นี้ เพราะจุดประสงค์หลักคือกันคนที่ **ไม่ได้ล็อกอินเลย** ออกจากระบบก่อน
(ตรงกับหลักการใน `CLAUDE.md`: "การซ่อนปุ่มตามบทบาทไม่ใช่ความปลอดภัย")
